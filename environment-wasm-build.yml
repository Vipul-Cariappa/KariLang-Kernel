name: xeus-karilang-wasm-build
channels:
  - conda-forge
dependencies:
  - cmake
  # emscripten 4.x: the emscripten-forge-4x packages the kernel links against
  # are built for that ABI.
  - emsdk >=4.0.9
  - empack >=3.2.0
