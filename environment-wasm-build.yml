name: xeus-karilang-wasm-build
channels:
  - conda-forge
dependencies:
  - cmake
  # emsdk is installed by the workflow instead: the conda-forge package stops at
  # 3.1.46, and the emscripten-forge-4x packages the kernel links against are
  # built for the emscripten 4 ABI.
  - empack >=3.2.0
